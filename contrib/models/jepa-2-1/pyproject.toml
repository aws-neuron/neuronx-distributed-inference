[project]
name = "jepa-2-1-neuron"
version = "0.1.0"
description = "V-JEPA 2.1 encoder port for AWS Trainium"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.1",
]

[project.optional-dependencies]
test = [
    "pytest>=7.0",
]

[tool.pytest.ini_options]
testpaths = ["test"]
